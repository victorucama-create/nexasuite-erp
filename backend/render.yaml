services:
  - type: web
    name: nexasuite-erp
    env: node
    buildCommand: |
      cd ../backend && npm install
      cd ../frontend && npm install && npm run build
    startCommand: cd ../backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        generateValue: true
    healthCheckPath: /api/health
    autoDeploy: true
    domains:
      - nexasuite-erp.onrender.com
